extends main.pug

block create
    .container
        .row
            a#viewMyStories(href="stories/"+"LOCALSTORAGEAUTHORID") show my stories
            div#storyDiv
                .row
                    ul
                        each story in stories
                            li #{stories.title}
                                #storiesDisplay.col.s12.m6
                                    .card.blue-grey.darken-1
                                        .card-content.white-text
                                            a#storyLink.card-title(href="write/"+story.id) #{story.title}
                
                                        .card-action
                                            a #{story.genre}
                                            a #{story.Author.name}
                                            
                    
